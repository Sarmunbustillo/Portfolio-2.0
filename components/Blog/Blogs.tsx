import styles from './Blogs.module.scss';

const Blogs = () => {
    return (
        <section id="blogs" className={styles.blogs}>
            <h2>Coming Soon</h2>
        </section>
    );
};
export default Blogs;
